## For importing libraries: numpy, simpleai

### Step1: Open CMD.

### Step2: Enter install command
(python -m pip install library_name)
```cmd
python -m pip install simpleai 
```

```cmd
python -m pip install numpy
```
